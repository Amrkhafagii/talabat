name: app_core
description: Shared services (Supabase, env, routing helpers) for Talabat Flutter apps.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  flutter_secure_storage: ^9.0.0
  go_router: ^14.2.0
  hooks_riverpod: ^2.5.1

