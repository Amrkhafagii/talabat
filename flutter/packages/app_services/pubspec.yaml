name: app_services
description: Cross-cutting services (auth, push, location, realtime, RPC repositories).
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod: ^2.5.1
  app_core:
    path: ../app_core
  firebase_messaging: ^15.1.3
  geolocator: ^10.1.0
  permission_handler: ^11.3.1
  supabase_flutter: ^2.5.6


dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
