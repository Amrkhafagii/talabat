{
  "name": "talabat-flutter-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "guard:react-native": "node scripts/guard-react-native.mjs",
    "test:contract": "node --test tests/contract/rpcs.test.mjs",
    "test:flutter": "cd flutter && flutter test && flutter test integration_test",
    "test": "npm run guard:react-native && npm run test:contract && npm run test:flutter",
    "build:supabase-openapi": "node scripts/build-supabase-openapi.mjs",
    "check:env": "node scripts/check-env.js",
    "diagnose": "npm run check:env && npm test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.2",
    "dotenv": "^16.4.7"
  }
}
