export function logMutationError(event: string, details: Record<string, any>) {
  // Placeholder telemetry hook; replace with real logging endpoint if available.
  console.warn('[telemetry]', event, details);
}
